# ============================================================
#  myAssistente v5 — pip dependencies
#  necessary for Python users only
#  Install with:   pip install -r requirements.txt
#  Generate with:  pip freeze > requirements.txt
# ============================================================

# --- Core — REQUIRED ---
pyttsx3                 # TTS (Text-to-Speech)
Pillow                  # avatar images (.jpg, .png, .gif)
opencv-python           # avatar video (.mp4)

# --- STT (Speech-to-Text) — optional, enables microphone input ---
SpeechRecognition
sounddevice
numpy

# --- TTS (Text-to-Speech) — LINUX only optional enables voice ---
espeak

# --- drag-and-drop — optional, enables drag and drop of links ---
tkinterdnd2

# --- AI — optional, choose ONE provider ---
# openai                  # OpenAI provider (GPT)
# google-generativeai     # Google Gemini provider

# --- NOTES ---
# aiml_parser: download the aiml_parser.py file from the repositoy
# insert it in the same folder as myAssistente5_3.py
#
# Standard Python libraries already included (no installation needed):
# tkinter, json, os, queue, re, shutil, subprocess,
# sys, random, threading, platform, pathlib, datetime, webbrowser
